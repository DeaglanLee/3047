<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('../partials/head'); %>
</head>

<body>
    <div class="page-container d-flex flex-column min-vh-100">
        <div class="bg-dark">
            <%- include('../partials/topnavbar') %>
        </div>
        <div class="content-container mx-5 mt-5 row">
            <!-- Hidden store id -->
            <div hidden id="store-id" data-store-id="<%= storeAdmin[0].store_id %>"></div>
            <div class="container-fluid d-flex justify-content-center align-items-center position-relative"
                style="height: 90px;">
                <h1 class="m-0">All Items for <%= storeAdmin[0].name %></h1>
            </div>
            <%- include('../partials/itemList', { includeStoreFilter: false, endpoint: '/api/store-items' }) %>
            
        </div>
    <div>
</body>

</html>
